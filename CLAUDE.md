# Pensieve

Personal (single-user) native-macOS tool that reconstructs *where each of my parallel projects stands* by automatically capturing work — git commits and Claude Code sessions — and surfacing grounded, provenance-cited summaries and a "what's next" queue. Built to help an ADHD workflow reload context on many efforts. **Not a product.**

## Status

- **Phase 1A — DONE** (merged to `main`): headless capture → ingest → query pipeline + `pensieve` CLI + git-hook installer. 19 tests.
- **Phase 1B — DONE** (merged to `main`): the intelligence layer (loose-end extraction, verbatim trust gate, on-device guided-generation LLM, grounded summaries, `next`/`digest`). **The make-or-break precision gate PASSED** on real transcripts (0 noise, 0 fabrication across 3 on-device acceptance runs). 56 tests. See `docs/superpowers/phase-1b-outcome.md`.
- **Phase 1B-org — DONE** (merged to `main`): the typed tree & strands. `Project→Node` rename + strict recursive tree (`parentID`/`kind`/`description`/`metadataJSON`/`branchKey`); git-common-dir source keying (unifies worktrees into one node); auto-birth *strand* nodes from git/session activity (tag-then-materialize, ≥2 same-kind events, lossless repoint of events **and** their loose ends); `SessionStart` hook + `SessionBranch` (captures a session's branch); on-device strand naming (best-effort, outside the trust gate); `group()` re-parents children; organizing CLI (`add-node`/`nest`/`rename`/`retype`) + tree-aware `list`. Additive migrations v4–v6; **trust gate untouched**. 75 tests. See `docs/superpowers/plans/2026-07-04-pensieve-phase1b-org.md`.
- **Pensieve.app v0.1 (heartbeat) — DONE** (merged to `main`): the read-only SwiftUI window over a shared, testable `MonitorSnapshot` kernel (pure gather, reads both stores read-only, never throws). **Superseded by the three-pane app below** — its `MonitorSnapshot` kernel lives on as the sidebar status footer.
- **Source discovery (`pensieve scan`) — DONE** (merged): filesystem scanner (`FileSystemSourceType`, `GitSource`). `SourceScanner.discover(root:recursive:db:)` walks write-free (dedup, prune); `accept(_:db:)` registers + installs hooks (best-effort). CLI: `pensieve scan <folder> [--recursive] [--accept]`. Settings window UI (pass 2) deferred.
- **Sync daemon — DONE & LIVE** (merged): closes the manual-ingest gap. `pensieve sync` = drain spool → `TranscriptDiscovery` (glob finished + in-progress transcripts) → drain again → incremental `ExtractionRunner.run()` (byte-size-gated). `SessionEnd` capture hook (precise trigger) + periodic launchd `com.pensieve.sync` LaunchAgent (`install-daemon`, one-shot every 300 s, `RunAtLoad`). Auto-drain + auto-extract now run unattended. **Superseded by the SMAppService bullet below** — the hand-installed `com.pensieve.sync` LaunchAgent + `install-daemon` command are retired.
- **Background sync via a bundled `SMAppService.agent` — DONE** (merged): retires the hand-installed `com.pensieve.sync` LaunchAgent for a code-signed agent bundled *inside* the app. A committed, **home-independent** plist (`Contents/Library/LaunchAgents/me.mazetti.pensieve.sync.plist`, `BundleProgram`, `StartInterval 300`, `ProcessType Background`) schedules a thin in-bundle helper `Contents/Library/Helpers/PensieveSyncAgent` that runs the *same* `SyncRunner`, exiting between runs. The helper resolves the machine-specific `PATH` (`SyncAgentEnvironment.resolvedPATH` — launchd does no `~` expansion) and writes `sync.log` itself at runtime (keeping `SystemStatus.lastSyncAt`'s mtime honest). App owns register/unregister/status via a thin `BackgroundSyncService` (Settings ▸ General toggle + status + open-Login-Items), **guarded off `.build` paths** (`BackgroundSyncGuard`) so a smoke-launch never touches real Login Items; it also boots out the legacy `com.pensieve.sync` on first launch. Runs independently of the GUI → no force-quit gap; on-device provider only (trust gate unchanged). **Gotchas:** the app MUST run from `/Applications/Pensieve.app` (SMAppService pins registration to **path + cdhash** — DerivedData isn't stable, and `rm -rf .build-xcode` would delete a registered bundle); each ad-hoc rebuild mints a new cdhash and a bare `register()` silently no-ops a stale registration (spawn-fails `EX_CONFIG`), so `registerIfNeeded()` does `unregister()` + `register()` to refresh the LWCR (approval persists across the cycle — no re-prompt). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-14-background-sync-smappservice-agent-design.md` + `2026-07-15-background-sync-smappservice-agent.md`.
- **Pensieve.app three-pane — slices 1–2 DONE** (merged): the real app (then run via `swift run PensieveApp`; now built via Xcode — see the app-bundle bullet below). **Slice 1** — read-only three-pane `NavigationSplitView`: action-first sidebar (Briefing + What's Next/Dormant/Recently Active smart lists + the typed node tree), middle node list, detail recall view (What It Is / Loose Ends with **inline verbatim provenance** / Recent Activity); drains the spool on launch; `NodeForest`/`SmartLists` are tested PensieveKit helpers, views are thin. **Slice 2** — **Briefing** home (by-project "since last visit" world map, default landing, backed by tested `BriefingQueries`; `lastOpenedAt` persisted in **UserDefaults**, not the canonical store) + **⌘K** navigation-only command palette. Window hosted via `NSHostingController` (titlebar safe-area). Design: `docs/superpowers/specs/2026-07-05-pensieve-app-three-pane-design.md`; plans `…-slice1-…` / `…-slice2-…`. **138 tests.**
- **Pensieve.app three-pane — slice 3a (LLM "Last Work Done" narration) DONE** (merged): the app's **first LLM call**. `DetailView` shows a grounded prose recap above Loose Ends, generated **on-device** (`makeDefaultLLMProvider()`), automatic-on-open + progressive (spinner→prose), cached per session (⌘R re-narrates). The trust-sensitive boundary is a tested PensieveKit `SummaryBuilder.narrate(project:events:) → String?` that returns **nil** (never a facts-dump) on no-events/provider-failure/empty — narration is best-effort, **outside the strict cited trust gate** (like strand naming), while loose ends stay cited. `SummaryBuilder` is `Sendable` so the `@MainActor` app awaits it off-main. The `.task` state machine (reset-on-node-change, `isNarrating` reset every entry, `Task.isCancelled` guard, **prose-first render gated on `loadedNodeID == node.id`**) prevents stale/wrong-node prose — hardened by **two independent adversarial spec reviews + an Opus whole-branch review** (one Important render-window bug caught + fixed). Rest of slice 3 (**3b**: `ValueObservation` liveness, ⌘⌥I inspector, window/materials polish) deferred. **150 tests.** Spec/plan: `docs/superpowers/{specs,plans}/2026-07-06-three-pane-slice3a-last-work-done*`.
- **Pensieve.app three-pane — slice 3b (liveness · inspector · recall windows) DONE** (merged to `main`): four parts. **(1) `ProvenanceContext` kernel** — tested read-only PensieveKit query resolving a loose end → its source `cc.session` event → the **surrounding transcript context**, with a two-part guard (`isUserPrompt` **and** the cited message still contains the stored `quote`) so a stale/compacted index never highlights the wrong message; degrades honestly (`transcriptAvailable == false` → stored quote + accurate note, never a fabrication). **(2) ⌘⌥I provenance inspector** — `.inspector` panel showing that context (cited highlighted, non-user/machine-envelope messages dimmed); `DetailView` gained `allowsInspector: Bool` gating the shared `AppModel.inspectedLooseEndID` write, which **clears on `selectedNodeID` change** — together preventing cross-window contamination. **(3) Secondary recall `WindowGroup(for: UUID.self)`** — ⌘⌥N opens a focused single-node recall window reusing `DetailView(allowsInspector: false)`; **strictly additive** to the single `Window("main")` + `MenuBarExtra` so the `pensieve://`/App-Intents deep-link bridge is untouched; per-window UUID = independent selection (native tabbing is system-pref-dependent). Plus a `SmartListKind.color` sidebar polish (system semantic roles, only surfaced buckets). **(4) Liveness** — retired the 3 s `Timer` for GRDB `ValueObservation` + two directory `FSEventStream` watches (spool incl. `-wal` → app self-drain; canonical incl. `-wal` → refresh, catching the external launchd daemon `ValueObservation` can't see cross-process), coalesced through a tested actor `Debouncer` (~150 ms), app-lifetime (not scene); canonical pool got a busy `.timeout`; Spotlight reindex folded into the debounced refresh (self-drain echo converges — a no-op drain writes no WAL frames). Built subagent-driven; **two independent adversarial spec reviews** (spool-`-wal` watch, app-lifetime teardown, `isUserPrompt` guard, shared-`inspectedLooseEndID` fix, `ValueObservation` redundancy) **+ per-task reviews** (caught the InspectorView stale-render race + flaky timing tests → deterministic injectable-sleep design) **+ an Opus whole-branch review** (READY-TO-MERGE, 0 Critical/Important). **173 tests.** Spec/plan: `docs/superpowers/{specs,plans}/2026-07-06-three-pane-slice3b-liveness-inspector-windowing*`.
- **Pensieve.app GUI base-state — DONE** (merged to `main`): replaced the imperative `NSApplication`/`NSWindow`/`NSHostingController` bootstrap with the first-party SwiftUI `App` lifecycle — single `Window` scene, standard menu bar + ⌘Q/⌘W/⌘M, window default/min size, automatic scene-based frame restoration. Menu `.commands`: View ▸ Show/Hide Sidebar (`SidebarCommands`), Go ▸ Quick Jump (⌘K, palette state hoisted to `AppModel`), Go ▸ Refresh (⌘R). Runtime **Dock icon** via `NSApplication.applicationIconImage` (artwork committed under `icons/`, source-of-truth for the future bundle). **Gotcha fixed:** an unbundled SwiftPM executable needs an explicit `setActivationPolicy(.regular)` in the `AppDelegate` or it has no Dock/⌘-Tab/menu bar. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-05-pensieve-app-gui-base-state*`. 138 tests.
- **Dogfooding — LIVE**: capture + auto-drain + auto-extract all running. The CLI ships inside `Pensieve.app`; `~/.local/bin/pensieve` is a **symlink** to `…/Contents/Helpers/pensieve`, auto-created on launch (when absent) and installable/repairable/replaceable via Settings ▸ General ▸ Command-line tool — no more "rebuild + reinstall the release CLI" step, updating the app updates the CLI; SessionStart + SessionEnd hooks in `~/.claude/settings.json`; background sync is owned by the bundled `SMAppService.agent` (`me.mazetti.pensieve.sync`) registered from `/Applications/Pensieve.app` and is **LIVE again since 2026-08-13T11:18Z**, running every 300 s. It had been dead since 2026-08-11T23:29Z (`Launch Constraint Violation`) because installs kept minting a new helper cdhash while the app actually being launched was a stale copy Spotlight picked — so `registerIfNeeded()` never refreshed the LWCR. **Launch the installed bundle with `make run`, never Spotlight**; `make install` alone leaves the registration stale. Resolution + the eight refuted hypotheses: `backlog.md` ▸ "Background sync is dead". Approve once in System Settings ▸ General ▸ Login Items; the app boots out the legacy `com.pensieve.sync` on first launch; real stores at `~/Library/Application Support/Pensieve/`. Watch: `tail -f ~/Library/Logs/Pensieve/sync.log`.
- **Xcode adoption — DONE** (merged): the app is now a real `Pensieve.app` bundle built by **XcodeGen** (`project.yml`) + Xcode 26.6 (active toolchain), linking PensieveKit as a local SwiftPM package; ad-hoc signed; bundled `icons/Pensieve.icon` via `actool`; `CFBundleName=Pensieve` fixes the menu title; the unbundled workarounds are gone. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-06-xcode-adoption-app-bundle*`.
- **Menu-bar item + `pensieve://` deep links (v0.2) — DONE** (merged to `main`): the first OS-integration surface. A `MenuBarExtra` `.window` popover in the *same* app (heartbeat over `MonitorSnapshot` + top-5 What's Next over `SmartLists`; status by glyph shape; jump-in), plus a minimal, tested `DeepLink` router (`Sources/PensieveKit/Support/DeepLink.swift`) for the registered `pensieve://` scheme (`briefing`/`node/<uuid>`/`smartlist/<kind>`). Routing (from adversarial spec review): internal clicks apply navigation **in-process** via `applyDeepLink`; external opens go through an `NSApplicationDelegateAdaptor` → `pendingDeepLink` → the always-mounted `MenuBarExtra` label's `.onChange` → the existing `PaletteDestination.apply(to:)` (no `.onOpenURL`, no Launch-Services round-trip). Scheme registered via an **XcodeGen-managed Info.plist** (`GENERATE_INFOPLIST_FILE` off; version/icon/name keys re-declared). **Additive only** (dock + Window untouched; the `LSUIElement`/hide-dock toggle is deferred to a Settings surface). C1 verified at runtime (external `pensieve://` fronts the app). **144 tests.** Spec/plan: `docs/superpowers/{specs,plans}/2026-07-06-menu-bar-deeplinks*`.
- **App Intents foundation + Spotlight (v0.3) — DONE** (merged to `main`): the second OS-integration surface, and the foundation every later App-Intents surface builds on. One `NodeEntity` (`AppEntity` + `IndexedEntity`) lights up **Spotlight content** (nodes searchable → tap opens the recall view), a parameterized **Show Pensieve List** intent (What's Next / Dormant / Recently Active) and an **Open Node** intent → **Siri + Shortcuts + Spotlight actions**, all via an `AppShortcutsProvider`. Derivation stays in a tested PensieveKit kernel (`NodeFacts`/`NodeFactsQueries`, active-only + by-id, read-only); the app-target intents/entity/indexer are thin. Intents run **in-process**, so `perform()` reuses the shipped `AppDelegate.receive` → `pendingDeepLink` bridge (no new nav path, no App Group). `SpotlightIndexer` does a **clear-then-index** of active nodes on launch + ⌘R (not the 3 s timer). **Deployment target 14 → 15** (required for `IndexedEntity`); `Package.swift` stays `.macOS(.v14)` (all App-Intents code is app-target-only). **146 tests.** Chosen over standalone Core Spotlight so Spotlight is a *conformance* under App Intents, not a parallel integration. Human-verify items (real Spotlight hit, Siri phrase, Shortcuts, cold-launch, stale-eviction) are the user's to run. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-06-app-intents-foundation*`.
- **Pensieve.app localization (String Catalog + German) — DONE** (merged to `main`): first-party Xcode **String Catalog** (`Sources/PensieveApp/Localizable.xcstrings`, English base + German `de`, 44 keys) localizing **all app UI chrome**; wired via `project.yml` (`options.developmentLanguage`/`knownRegions [en,de]`, `CFBundleLocalizations`, `SWIFT_EMIT_LOC_STRINGS`); impersonal/infinitive German. **Chrome only** — node names, loose-end quotes, descriptions, event summaries, roles, transcript text are **never** localized (content-mixed frames + the proper names "Pensieve"/"Briefing" stay English-fallback). **Gotcha:** `xcodebuild … build` does **not** auto-populate the source `.xcstrings` with extracted keys (IDE-only) — author/reconcile keys by hand against the Swift literals (`%lld`/`%@`); a mis-keyed `de` value silently falls back to English. Verify: `de.lproj/Localizable.strings` in the bundle + `plutil -p` + forced-locale launch (`-AppleLanguages '(de)'`/`'(en)'`). Subagent-driven; Opus whole-branch review READY-TO-MERGE (0 findings). **Out of scope / deferred follow-up:** App Intents / Siri / Shortcuts phrases + the `pensieve` CLI. **Human-verify carry:** native-speaker in-situ tone pass (e.g. "Moved→Bewegt", "capturing→erfasst"). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-06-app-localization-german*`. 173 tests (Kit unchanged).
- **Pensieve.app three-pane — slice 4 (in-app organizing writes) — DONE** (merged to `main`): the app can now reorganize the typed tree. Five ops via native `.contextMenu` on **both** sidebar-tree and middle-list rows + toolbar "+"/File ▸ New Node (⌘N): **New Child · Rename · Change Type ▸ (all 7 kinds) · Move to… · Merge into…** (destructive, `.confirmationDialog`). In-place rename is a focused `TextField` (Enter/blur commit, Esc cancel); creation renames in the flat middle list (OutlineGroup can't be force-expanded). **Two load-bearing PensieveKit correctness fixes (tested):** a unified **guarded `NodeCommands.reparent`** (walk-to-root cycle guard; `nest` now routes through it — closes the pre-existing missing guard) and a **general cycle-safe `ProjectResolver.group`** (lifts the survivor above the highest absorbed node on its own ancestor chain → no self/transitive cycle for *any* merge incl. multi-ancestor-in-one-call; the initial parent-into-child-only fix was caught insufficient in review and generalized) + a pure `NodeForest.descendantIDs` picker guard (Move/Merge exclude self+descendants). App writes are thin on `AppModel` (op → explicit `refresh()`; Node-only writes don't trip the Event-count `ValueObservation`); `merge` moves selection/rename state off the deleted source; default new-node **name is NOT localized** (content → canonical store). Subagent-driven (Sonnet impl/review, **Opus** whole-branch = READY-TO-MERGE, 0 Critical/Important). **182 tests** (+9 Kit). Rebased onto mid-session main (xcode-scheme + inspector-perf commits) and the merge folded slice-4 keys into the Xcode-reformatted String Catalog (66 keys). **Human-verify carries** (need the built app, real store, plain `open`): rename commit/cancel in place; Change Type icon updates; Move/Merge pickers **never list the node or its descendants** + "Top level"; merge confirmation copy + merged activity re-homes under the target; `pensieve list` matches the app tree after each op; German in-situ. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-07-three-pane-slice4-organizing-writes*`.
- **Pensieve.app chrome polish batch — DONE** (merged to `main` `0a7eb0f`): ten **app-target-only** tasks (no PensieveKit changes; 196 tests unchanged) making the app feel Apple-native, from backlog "App UX & IA polish carries" items 1–4 + dogfooding refinements. **Wave 1:** reading-prose typography (`ProseStyle.swift` — 14pt / lineSpacing / 680pt measure cap); window toolbar actions moved onto the **detail column** (New Node "+" leading; Refresh + Inspector trailing) behind a single **native** sidebar toggle; native sidebar status bar (hairline over `.background(.bar)`); Reminders-parity New/Edit modal (`IconPicker.swift`) with anchored SF-Symbol **search** + emoji picker popovers. **Wave 2:** consistent type scale + balanced activity timeline; inspector renders transcript **Markdown** via MarkdownUI 2.4.1 (xcodebuild-only dep — kept SwiftPM-clean so `swift test` is unaffected; discard the transient `Package.resolved` churn after an xcodebuild); content-column header ("Pensieve / N Projects"); layout robustness (firm column bounds sidebar 220–340 / content 280–460 / detail min 380 + window min 900×480 / default 1040×660; inspector Markdown horizontal-overflow wrap; 12pt meta / 14pt day-header font bumps). Subagent-driven (Sonnet impl+task-review each); **two Opus whole-branch reviews** → READY-TO-MERGE (0 Critical/Important; a final pass over the post-review T9/T10 delta cleared it too). **Human-verify carries** in `CONTINUE.md`. Spec/plans: `docs/superpowers/{specs,plans}/2026-07-07-app-chrome-polish-*` (+ `…-wave2.md`).
- **Pensieve.app Focus filters (`SetFocusFilterIntent`) — DONE** (merged to `main` `171c0e6`): the ⭐ user-flagged high-value App-Intents surface. When a macOS Focus is active, Pensieve restricts the main window + menu-bar popover + Spotlight to the projects/strands matching that Focus's context. **Kit (tested):** migration **v9** `nodes.context` (`work`/`personal`/`""`unset); `NodeContext.swift` — constants + pure `NodeContextResolver` with **subtree inheritance** (nearest non-empty ancestor wins; child overrides) + a single **visibility predicate** `visibleNodeIDs(for:in:)` (mute the opposite explicit context, always show unset, `""` ⇒ all); `NodeCommands.add/update` grow a defaulted `context`. **App (thin):** `PensieveFocusFilter: SetFocusFilterIntent` with an **optional** `@Parameter` (nil delivered on deactivation → persists `""` → unfiltered) → `UserDefaults` (`FocusFilterDefaults.activeContextKey`); `AppModel` seeds on launch + observes `UserDefaults.didChangeNotification` (main-queue hop) → re-filters `lists`/`briefingCards`/`forest` (forest rebuilds on context-only change via a `lastForestContext` guard; **`allNodes` stays the FULL set**) + reindexes; `SpotlightIndexer.reindex(activeContext:)` filters its index; a Context picker (Work/Personal/Unset) in the New/Edit modal; German l10n. Menu-bar filtered too (shared `lists` state; user-approved deviation). Subagent-driven (7 tasks: Sonnet impl+task-review each; **Opus** whole-branch = READY-TO-MERGE, 0 Critical/Important, 2 Minors fixed). **204 tests** (+8 Kit). Accepted trade-offs + human-verify carries in `CONTINUE.md`. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-07-focus-filters*`. **⚠️ BLOCKED AT RUNTIME by ad-hoc signing (found 2026-08-11) — the code is correct but the filter cannot be attached.** System Settings ▸ Focus ▸ App-Filter shows Pensieve and renders the Context picker (all from static `Metadata.appintents`), but **"Add" stays permanently disabled**: `linkd` can't build a bundle identity for an ad-hoc-signed app (`Failed to generate bundleIdentity: -Not a platform binary, checking teamId... -Unable to get teamId`) and so rejects every connection from it (`Rejecting invalid client due to requiresValidatedBundle`); the app's callback to `com.apple.linkd.mediator` fails (`NSCocoaErrorDomain 4097`), the sheet reports `LNConnectionErrorDomain 1200 "AppleEvents communication failed."`, and with no prepared instance the button never enables. Verified against Microsoft Outlook as a control on the same OS (`TeamIdentifier=UBF8T346G9`, 0 rejections, Add enabled *even with no value picked*), so this is **not** the optional-`@Parameter` design and **not** the reported macOS 26.5 selection regression. **A Team ID is required and only Apple-issued certs carry one** — `man codesign`: "Team identifiers are automatically recorded in signatures when signing with suitable Apple-issued signing certificates"; a self-signed cert with a 10-char `OU` was tested and yields `TeamIdentifier=not set`, so there is **no local workaround**. Same root cause floods `com.apple.linkd.auto` with ~2,500 rejected connections/second during `SpotlightIndexer` donation, so **entity Spotlight indexing is likely also non-functional here** (unverified — the donated-entity index isn't `mdfind`-queryable). Unblocking needs an Apple Development cert (free Personal Team is plausible but **unverified** — needs interactive Apple ID sign-in) or a paid membership, which would also clear the Widgets/CloudKit gate — the same signing gate already deferring Widgets and CloudKit.
- **Pensieve.app Settings surface (first cut) — DONE** (merged to `main` `9de6d18`): the app's first `Settings` scene (**⌘,**), three knobs. **Kit (tested):** `ProviderPreference` + `Preferences` (JSON in the shared, **non-sandboxed** support dir, best-effort → `.auto`) + `PensievePaths.preferencesURL()`; a pure `resolveProviderKind`; `makeDefaultLLMProvider(prefsURL:)`/`defaultProviderKind(prefsURL:)` read the persisted choice (explicit URL → `PENSIEVE_PREFS` env → support dir) so **both the app AND the launchd daemon** honor it. **App (thin):** `SettingsView` reads/writes `Preferences` directly (no `@Published` mirror) + shows Foundation Models availability; **rebuildable `AppModel.summaryBuilder`** (`rebuildSummaryBuilder()`) so an in-app provider switch takes effect without relaunch; `AppDefaults` shared UserDefaults keys; `AppDelegate.applicationDidFinishLaunching` applies the activation policy — **hide-Dock via `.accessory`, closing the v0.2-deferred `LSUIElement` item**; `DetailView` gates narration render+generation on the toggle; German l10n. **Trust gate untouched** — this only selects *which* provider runs behind it. Two independent adversarial spec reviews (lazy-`summaryBuilder` no-op, injectable `prefsURL` to dodge a parallel-test env race, `applicationDidFinishLaunching`+`NSApp.activate`) + subagent-driven (6 tasks) + **Opus** whole-branch review; then a **high-effort `/code-review`** caught **two** real defects the whole-branch review missed (Share leaked disabled narration; `NarrationCacheKey` wasn't provider-keyed) — both fixed. **246 tests** (+9 Kit). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-08-app-settings-surface*`. **Deferred:** cloud/API `LLMProvider` + Keychain key + model selection — **now DONE** (next bullet); organizing-writes error surfacing — **now DONE** (see the Settings v2 bullet below).
- **Cloud/API LLM provider (Settings follow-up, Track B) — DONE** (merged to `main` `34a2bb9`): an **app-only** cloud (HTTP) `LLMProvider` as a fourth provider option for the app's best-effort "Last Work Done" narration — **Anthropic + OpenAI-compatible**, one struct with a flavor switch. **Kit (tested):** `CloudFlavor`/`CloudConfig` + pure `CloudHTTP` request builders & response/model-list parsers (per-flavor path suffixes that never synthesize/strip `/v1`; trailing-slash-safe) + `CloudLLMProvider` (`complete` + static `listModels`) over an **injected transport** (the default URLSession transport guard-casts to `HTTPURLResponse`, never force-casts); `KeychainSecretStore` (generic-password wrapper, `service=com.pensieve.cloud-llm`, `account=flavor.rawValue` so both keys coexist). **Storage reworked:** retired `preferences.json` — provider selection + non-secret cloud config now live in **UserDefaults** (`me.mazetti.pensieve`), read **cross-process** by the CLI/daemon via `PensieveDefaults.shared()` (`UserDefaults(suiteName:)`; the app is not sandboxed) — **no daemon regression** (a `.cloud` selection the keyless CLI reads falls back to local; every other selection is honored). Pure `resolveProviderKind(…cloudConfigured:)` + a shared `resolvedProviderKind(defaults:cloudConfig:apiKey:)` single-source-of-truth behind `makeDefaultLLMProvider(defaults:cloudConfig:apiKey:)`. **The API key is Keychain-only — never UserDefaults/plist/JSON/log.** **App (thin):** a Settings cloud subsection (flavor / base URL / key `SecureField` / model picker + **Fetch** = populate the model list *and* validate the key/connection) with inline error; the key commits on submit **and on close** (guarded so an unchanged key isn't rewritten); the model resets on flavor switch; the narration cache key folds `cloud:flavor:model` only when the resolved kind is `"cloud"`; German l10n (vendor names stay English). **Trust gate untouched** — cloud serves only narration; extraction stays on-device. Subagent-driven (7 tasks + 1 final-fix wave; Haiku/Sonnet impl + per-task review; **Opus** whole-branch = READY-TO-MERGE, 1 Important + 4 Minor → all fixes applied + re-reviewed clean). **260 tests** (cloud suite `CloudProviderTests` + reworked `Preferences`/`DefaultProvider` tests; the parallel salience-gate commit `017c354` folded in on rebase). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-08-cloud-llm-provider-design.md` + `2026-07-09-cloud-llm-provider.md`. **Out of scope (by design):** cloud extraction, streaming, per-request cost/telemetry, daemon/CLI cloud use. **Post-merge carry:** rebuild + reinstall the release CLI (the provider-selection read changed) + the human-verify list in `CONTINUE.md`.
- **MCP context server (`pensieve mcp` + `pensieve prime`) — DONE** (merged): Pensieve feeds its grounded context back into Claude Code across three surfaces over one tested Kit kernel (`SessionContextQueries`). **`pensieve mcp`** — official MCP Swift SDK (`0.12.1`, pensieve target only) stdio server: tools `project_context` (roots/cwd-scoped, `node_id` override) + `whats_next` (ranked, `context?` filter), and user-attachable resources `pensieve://node/{id}` + `pensieve://smartlist/whats-next` (`text/markdown`). **`pensieve prime`** — a SessionStart hook printing the cwd's compact bundle (cache-read-only prose: never narrates/spawns/blocks; nothing on an unbound cwd). New disposable `NarrationCache` (`narration-cache.sqlite`, keyed `NarrationCacheKey.make(events:provider:)`) shares prose across app/CLI/MCP; `mcp` write-through warms it, `prime` reads it. Trust gate intact (quotes cited, prose best-effort `nil`). Register: `claude mcp add pensieve -- pensieve mcp`; and a SessionStart hook running `pensieve prime` matched on `startup`/`resume`/`clear`/`compact`. Deferred: MCP prompts (fast-follow), sync-daemon ambient-prose warming. Spec/plan: `docs/superpowers/specs/2026-07-08-mcp-context-server-design.md` + `docs/superpowers/plans/2026-07-10-mcp-context-server.md`. **Follow-up — `recall` tool DONE** (merged, 2026-07-11): a loose-end-keyed read-only MCP tool returning the **surrounding transcript window** (not just a pointer) via the tested `ProvenanceQueries` kernel — model-controlled `radius` (default 8), verbatim, inside the trust gate; `project_context` loose ends grow an additive `id` handle. Closed the "pointers, not passages" ceiling `pensieve mcp` first exposed. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-11-mcp-recall-tool*`.
- **In-app find (search captured content) — DONE** (merged to `main` `02bb0a1`, 2026-07-11): a native `.searchable` + **⌘F** search over the grounded core corpus (node name/description + open loose-end text/quote), grouped results in the content column, **land on the cited row** (auto-expand + scroll), Focus-scoped, trust-gate-clean. The **⌘K palette is retired** (nav core `PaletteDestination`/`applyDeepLink` retained for deep links/App Intents/menu bar). **Kit (tested):** `SnippetMaker` (unicode-safe highlight triple) + `SearchQueries` (grounded corpus, Focus scoping, deterministic ranking, `prefix(50)` cap). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-10-in-app-find*`. **Deferred siblings:** **1b** index loose-end text into Spotlight; **#2** semantic/vector recall (reuses this corpus).
- **Archive nodes — DONE** (merged to `main` `829c20e`, 2026-07-14): the escape hatch for stale work `delete` refuses (any node with a live git/session source, which would resurrect on the next drain). Archiving archives the **whole subtree**; archived nodes leave every normal view (tree, middle list, project count, Smart Lists, Briefing, Spotlight, in-app search) into a **collapsed "Archived" sidebar section**. **Snooze semantics:** a new commit / Claude session attributed to an archived node flips it **and its ancestor chain** back to `active` (ingest path only; a `git.checkout` doesn't resurrect). No migration (reuses the latent `Node.state`); `muted` stays deferred. Kit: `NodeCommands.archive`/`unarchive` (subtree write; unarchive walks the ancestor chain, symmetric with ingest resurrection). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-11-archive-nodes*`.
- **App Settings v2 + organizing-writes error surfacing — DONE** (merged to `main` `d1fab6f`, 2026-07-14): **closed Track B's second thread.** Native **tabbed Settings** shell (**General · Intelligence · Advanced**) over a tested `SystemStatus` gather kernel; the Advanced tab adds status readouts + store paths; About Pensieve is now the first-party `orderFrontStandardAboutPanel`. **The six organizing writes no longer `try?`-swallow:** each classifies its real return into a **refusal** (non-success = stale state → refresh + non-alarming alert) or a **failure** (a throw → alert, no refresh) via `AppError`/`@Published presentedError` → **one `.alert` on `RootView`**; archive/unarchive route through the same helpers. German l10n. Trust gate untouched. **400 tests** at merge. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-12-app-settings-v2-error-surfacing-design.md` + `2026-07-13-app-settings-v2-error-surfacing.md`. **Deferred (Spec 2, own brainstorm):** source-management GUI (wrapping `pensieve scan`) + sync-daemon interval editing.
- **Bundle the `pensieve` CLI into the app — DONE** (merged to `main`, through `3a9092c` 2026-07-18): the CLI is now an embedded Xcode **tool target** (`PensieveCLI`, `PRODUCT_NAME=pensieve`) at `Pensieve.app/Contents/Helpers/pensieve`; `~/.local/bin/pensieve` is an **app-managed symlink** to it, auto-created on launch (guarded off `.build`) and installable/repairable/replaceable via **Settings ▸ General ▸ Command-line tool** (tested `CLIToolInstaller` kernel). **Updating the app updates the CLI** — the "rebuild + reinstall the release CLI" step is retired. **`swift run pensieve` no longer exists** — build via `xcodebuild -scheme PensieveCLI` (or the app scheme, which embeds it). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-16-bundle-cli-into-app-design.md` + `2026-07-17-bundle-cli-into-app.md`.
- **Semantic / vector recall (Track C #2) — DONE** (merged to `main` `e640645`, 2026-07-18): the "find without exact words" feature, across ⌘F and MCP over one shared Kit kernel. **Native `NLContextualEmbedding`** (per-token → mean-pooled → unit-normalized; by-script Latin for EN+DE) + **`sqlite-vec`** — vendored as a static SwiftPM C target (`Sources/CSQLiteVec`, `sqlite3_auto_extension` is disabled on Apple platforms, so it registers **per-connection** via GRDB `Configuration.prepareDatabase` → `pensieve_sqlite_vec_init_connection`). Stored in a **separate, rebuildable, never-synced `semantic-index.sqlite`** (`vec0` with `node_id`/`state` metadata columns + `items`/`meta` tables; drop+rebuild on embedder-version/dimension change; busy `.timeout(5)` like the canonical pool). **Kit (tested):** `TextEmbedder`/`NLContextualEmbedder`, `SemanticIndexStore`, `EmbeddableItem`+`SemanticIndexer` (**membership/metadata-driven** reconciliation — prune by live `LooseEnd.isOpen`/`state`, repoint updates `node_id` without re-embedding, embedder-nil skips-and-retries not starves), `SemanticQueries` (**over-fetch `k'=max(k*8,50)`** + `state` in-KNN filter, then `visibleNodeIDs`/floor post-filter; the canonical join **re-applies the live corpus predicate** as the last grounding defense). v1 corpus = active nodes + open loose ends + enriched events (`EmbeddableItem` seam makes transcripts/future sources additive). **Grounded-retrieval-only** (real cited items; vector only ranks; on-device only — cloud never used) and **best-effort** throughout (missing asset/store → empty, never blocks capture/ingest). **App (thin):** ⌘F "Related" section (semantic-only, deduped vs exact, floor 0.25) + app-side index sync in `drainThenRefresh`; default-on Settings ▸ Intelligence toggle (`PensieveDefaults.semanticSearchKey`, honored by app/daemon/MCP); German. **MCP:** a unified `search` tool (exact+semantic blend, subsumes the deferred keyword sibling). Indexer runs after extraction in `SyncRunner` (daemon) + app refresh, toggle-gated. Subagent-driven (Task 0 sqlite-vec GO/NO-GO spike + 8 tasks; Sonnet impl+task-review each; **Opus** whole-branch = READY-TO-MERGE, 0 Critical, 1 Important fixed = busy timeout; two real defects caught+fixed mid-flight — embedder-nil starvation, a non-failing over-fetch test). **439 tests.** Spec/plan: `docs/superpowers/{specs,plans}/2026-07-18-semantic-vector-recall*`. **Deferred (fast-follow):** expand-and-retry on heavy Focus-muting; idempotent rebuild guard on version bump; MCP per-call embedder/store caching; transcript-passage chunking (the next corpus increment). **Post-merge carry:** rebuild + reinstall the app to `/Applications` for the bundled `pensieve mcp` to expose `search`.
- **Spotlight loose-end indexing (Track C 1b) — DONE** (merged to `main` `c60e624`, 2026-07-19): the OS-index counterpart to in-app ⌘F — open loose ends are now indexed into macOS Spotlight (previously nodes only), so a phrase from a loose end's **text or its cited `quote`** returns that specific item; selecting it opens Pensieve at the loose end's node with the cited row expanded. **Mirrors the proven `NodeEntity` Spotlight path** end-to-end. **Kit (tested):** a new `DeepLink.looseEnd(UUID)` case (`pensieve://looseend/<uuid>`, round-trip invariant) + `LooseEndFacts`/`LooseEndFactsQueries` (mirrors `NodeFacts`; `all` = open (`LooseEnd.isOpen`) loose ends in **active** nodes joined to node name; `facts(for:)` = degrade-safe **any-state** by-id resolution, quarantined to tap resolution so nothing stale leaks into the searchable corpus). **App (thin):** `LooseEndEntity: AppEntity, IndexedEntity` (`contentDescription` = text + verbatim quote → the quote phrase is findable) + `LooseEndEntityQuery`; `OpenLooseEndIntent` **registered in `PensieveShortcuts`** (load-bearing — an `IndexedEntity` is only Spotlight-surfaced when referenced as an App Shortcut); `SpotlightIndexer` indexes loose ends **Focus-scoped to the same `visible` set** as nodes in one clear-then-index pass; `AppModel.openLooseEnd` + `applyDeepLink` route `.looseEnd` via a **failable `PaletteDestination.init?`** that preserves the compile-time no-drift guard (a future `DeepLink` case fails to compile). **App-Intents strings stay English literals (NOT localized), matching `NodeEntity`/`OpenNodeIntent`** — no String Catalog change. Grounded (only real stored `text`/`quote`, no LLM) + best-effort + read-only throughout; a stale/deleted entry degrades honestly (opens node, else briefing, never crashes). Subagent-driven TDD (6 tasks; one reorder — Task 1's DeepLink case breaks the app's exhaustive switch, so the Task 6 routing fix ran early to unblock the build); per-task reviews clean; **Opus** whole-branch = READY-TO-MERGE, 0 Critical/Important; `/simplify` applied 1 cleanup (`openLooseEnd` reuses `AppModel.db` instead of a per-tap connection). **445 tests** (+6). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-19-spotlight-loose-end-indexing*`. **Human-verify carries** (need the built app @ `/Applications` + real store + plain `open`): real Spotlight hit on text vs quote-only; Focus muting; since-closed stale-entry degrade; German entity type name falls back to English "Loose End" (intended). **Post-merge carry:** rebuild + reinstall to `/Applications` so the bundled binary exposes the new intent/entity.
- **App-quality cleanup pass — DONE** (merged to `main`, 2026-07-19): three deferred code-quality carries, closed together. **(1) `NodeKind` + `NodeState` → real `RawRepresentable` enums** (`QueryBindable`; exhaustive `switch`es; kills the mistyped-literal hazard the archive-nodes review flagged) — **on-disk format byte-identical** (same strings, same STRICT columns, no migration; all SchemaV4–V9 tests pass); event kinds (`CaptureKind`, on the sacred capture path) stay `String` by design (there is **no** `EventKind` type). **(2) `AppModel.pruneNarrationCache`** drops cache entries for deleted/merged-away nodes on `refresh()` (intersect keys with the live `allNodes` set) so the plist can't grow unbounded; uses the **FULL** set so Focus-muted/archived nodes keep their cache. **(3) `AppModel` → `@Observable`** (`ObservableObject`/`@Published` retired; injection `@StateObject`→`@State`; consumers `@ObservedObject`→plain `var`, `RootView`→`@Bindable`; non-UI infra `@ObservationIgnored`) so SwiftUI scopes invalidation to the properties each view reads — a no-behavior modernization. (1)+(2) ran as parallel focused subagents; (3) subagent-driven with an **Opus** whole-branch review that caught **1 Important** — `allNodes` wrongly `@ObservationIgnored` left `RecallWindowView`'s body with zero observation deps, breaking cold-restore recovery — **fixed** (`allNodes` is read by view bodies via `node(_:)`, so it belongs tracked). **445 tests** (Kit unchanged; the app target has no unit tests → verified via build + smoke + eyeball). Plan: `docs/superpowers/plans/2026-07-19-appmodel-observable-migration.md`. **Human-verify carry:** the invalidation-sensitive eyeball checklist (esp. the **cold-restore recall window**: ⌘⌥N → quit → cold relaunch → resolves its node, no longer stuck on "Project unavailable").
- **Semantic-recall hardening + include-archived ⌘F search — DONE** (merged to `main` `3ece8b5`, 2026-07-19): four small robustness/UX items over the shipped semantic stack. **(A) Include-archived toggle (EXACT search only):** a **defaulted** `SearchQueries.search(includeArchived:)` param widens exact ⌘F to archived nodes + their open loose ends; app-side `AppModel.SearchScope` (`.active`/`.all`) + a native `.searchScopes` bar (Active / Include Archived) on the searchable `ContentListView`; `runSearch` snapshots the scope into a pre-Task local and passes it to `SearchQueries` **only** — **semantic "Related" stayed active-only** at merge time because the semantic index held **no** archived content (`EmbeddableCorpus.gather` was active-only; closed by the archived-semantic-index bullet below). One hand-authored String Catalog key ("Include Archived"→"Archivierte einschließen"). **(B) Rebuild-invariant test (test-only, no prod change):** pins that a same-version reopen of `SemanticIndexStore` preserves the index — the rebuild-on-mismatch guard already ships **and is already race-safe** (GRDB `pool.write` is implicitly `BEGIN IMMEDIATE`; the originally-planned transaction "fix" was proven a **no-op** by adversarial review against the vendored GRDB source and dropped). **(C) MCP embedder/store caching:** `PensieveMCP` hoists `NLContextualEmbedder`+`SemanticIndexStore` into `private static let` (once-only, `Sendable`) instead of rebuilding per `search` call (session-scoped caveat documented). **(D) Expand-and-retry under Focus-muting:** `SemanticQueries.search` wraps its KNN over-fetch in a grow-`k` loop (×4, cap 2000) with a **floor-aware early exit** (KNN is similarity-descending, so once the farthest neighbor is below floor no deeper fetch can help → sparse queries still fetch once); filter body extracted into a private `buildHits` preserving **every** grounding guard (floor/visible/exclude/canonical-join). **Trust gate untouched.** Design-first: **2 adversarial spec reviews** (caught the false Part B premise) + a **plan-time code check** (caught the exact-only scope gap — the index has no archived rows) → subagent-driven Tasks 1–4 (Sonnet impl+task-review each; **Opus** whole-branch = READY-TO-MERGE, 0 Critical/Important; 1 Minor doc-comment fixed) → Task 5 (app toggle) inline (build+smoke; interactive spike is a human carry). **448 tests** (+3 Kit). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-19-semantic-recall-hardening*`. **Deferred (rolled up, not foreclosed):** archived content in the semantic index / "Related" (needs a corpus-producer change); transcript-passage chunking; 5 **pre-existing** `SemanticQueries.resolve` trailing-closure warnings (left alone per surgical-change discipline). **Human-verify carries** (built app @ `/Applications` + real store): does the `.searchScopes` bar actually render under `.sidebar` placement? (fallback = a segmented Picker in the results header, pre-specified in the plan) · toggling includes/excludes archived · German in-situ. **Post-merge carry:** rebuild + reinstall to `/Applications` so the bundled `pensieve mcp` gets the Part C caching and the app shows the toggle.
- **Archived content in the semantic index / "Related" — DONE** (merged to `main`, 2026-07-19): closes the deferred item the semantic-recall-hardening batch left open — archived work is now recallable through semantic search, not just exact ⌘F. **Kit (tested):** `EmbeddableCorpus.gather` now indexes archived nodes and their loose ends/events too (tagged with the owning node's live `state`); `SemanticIndexStore.knn` and `SemanticQueries.search` grow an **allow-list** `includeArchived` flag (defaulted `false`); `resolve`'s canonical re-check predicate widens in lockstep so a stale/pruned row can't leak through; `SemanticHit` (and `NodeHit`/`LooseEndHit`) carry `isArchived` for the caller to badge. **App:** the existing ⌘F Include Archived scope now drives **both** the exact and semantic halves (previously exact-only) and badges archived rows in Related too. **MCP:** the `search` tool's `include_archived` parameter now reaches semantic results, and its JSON `SearchItem` carries the `archived` flag through all three hit types (a final whole-branch review caught this dropped at the MCP boundary — fixed). Trust gate untouched — this only changes which live rows are eligible to be returned, never what may be said about them. **465 tests** (+1 Kit). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-19-archived-semantic-index*`. **Post-merge carry:** rebuild + reinstall to `/Applications` so the bundled `pensieve mcp` and app pick up the widened search.
- **Transcript readability (chat rendering + harness vocabulary + type scale) — DONE** (merged to `main` `4b184a3`, 2026-07-26): the inline provenance view now renders a transcript window as readable chat instead of a flat list of raw-tagged text. **Kit (tested, pure):** `TranscriptVocabulary` — deliberately **two members, not one list**: `harnessTagNames` (renderer, safe to extend) vs a **FROZEN** `injectionMarkers` (moved verbatim out of `TranscriptParser.isInjectedOrCommand`, pinned by test) so a tag added for **display** can never silently change **extraction**; a third member `gateExemptTagNames` records, per tag, *why* the gate's behavior is intentional. `TranscriptSegment`/`TranscriptCallout`/`HarnessBlock`/`TaskNotificationBlock` + a 3-case `CalloutSeverity` (narrowed 6→3 on corpus evidence). `TranscriptMarkup` — a **single left-to-right scan where the outermost construct wins** (a two-pass harness-then-callout pipeline is self-contradictory: a callout containing a `<system-reminder>` would split into unpaired fragments and vanish); precedence **code → callout → harness → placeholder → orphan close → prose**, with CommonMark-pinned fence/indent/inline-code protection, forward-only pairing (an orphan close never pairs backward and swallows earlier content), and a **no-loss property** (every input byte survives into some segment). Harness envelopes parse their modelled children **losslessly** (`unrecognisedChildren` keeps what isn't modelled). `SpeakerClass.of` is **conjunctive on purpose** — `isUserPrompt` is a bare `contains`, and 309 genuine `type:"user"` records in this repo's own transcripts trip it (debugging this feature means pasting envelopes into chat), so a disjunctive rule would have the app assert the user didn't write something they did; unknown roles fall back to `.system` (`role` is not a closed set). **App (thin):** `TranscriptSegmentView` (segment → markdown / callout / harness card + the calmer type scale) + `TranscriptMessageView` (speaker classification → bubble vs strip + cited-provenance highlight); `LooseEndRow`'s **two** render paths delegate to them and gain `compact: Bool`, threaded through **three** call sites (`ContentListView` ×2 `true`, `DetailView` `false`). Chrome localized en+de (severity/harness/role labels); **tag names, message text, quotes, command names and task summaries stay verbatim content**. **Trust gate untouched** — `injectionMarkers` is byte-identical, and the renderer has no write path into `isUserPrompt`. **Notable spec deviation:** suppressed `<NAME>` placeholders are **backslash-escaped, not left bare** — bare `<NSError>` is valid CommonMark raw HTML that MarkdownUI swallows, so losing the text would be worse than the bug being fixed. Design-first: 3 adversarial spec reviews (the third verified against code, and caught that "one shared vocabulary constant" would have handed the renderer a write path into the trust gate) + a corpus re-measurement that **invalidated the first draft's evidence base** (the draft grepped 17.5% of transcripts, and both earlier reviews re-measured over raw JSONL rather than parser-visible text — any future re-measurement must reproduce `TranscriptParser.extractText`). **524 tests** (+59 Kit). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-19-transcript-readability*`. **Human-verify carry:** the Task 8 eyeball matrix (built app + real store) — cited-provenance bar on the **correct** message in all three speaker classes, heading scale, German in-situ. **Deferred siblings** (own specs, `backlog.md`): rich code blocks (syntax highlighting + Graphviz-DOT/Mermaid diagrams) · macOS Writing Tools on loose ends.
- **Retrieval P1 + P2′ — BM25 replaces the search engine — DONE** (2026-08-11): **closed the inert-semantic-floor defect by discarding its diagnosis.** It was a **ranking failure, not a scale failure** — mean-pooled `NLContextualEmbedding` was never a sentence-similarity encoder, so no floor (absolute, centered, or percentile) could have separated a topical match from gibberish. **P1 (corpus hygiene):** `EmbeddableCorpus.gather` drops `git.checkout` events and **per-node** duplicate texts (deliberately not global — collapsing "fix ci" across three projects would silently pick which project owns the only findable copy: a grounding call, not hygiene). **P2′:** a new FTS5 index (`SearchIndexStore` → `search-index.sqlite`, schema v2, `remove_diacritics 2`, hash-guarded whole rebuild) + `FTSQueryBuilder` (pure; **raw input never reaches `MATCH`** — quoting neutralises every operator) replaces **both** the old whole-string substring matcher **and** the vector index as the default engine. The vector stack is retained, tested, and default-**OFF**. **The floor is gone, not retuned** — BM25 scores are unbounded and per-query-scaled, so a fixed cutoff was never meaningful for it either; **a rank cap is not a relevance threshold**. **File paths join the corpus** (`Event.detailJSON["files"]`, captured since 1A and never searched) so "what was I doing when I last touched `SyncRunner.swift`" works — reachable via ⌘F and MCP `search`'s structured `file` parameter. **One ranked list with a pinned Top Hit** across ⌘F and MCP (which now also returns an `index_state`, so an unbuilt index is distinguishable from a real miss). **The pre-registered verification gate REJECTED the plan's own primary design**: paths beside text in one table regressed P@1 0.395→0.378 (n=300 indecisive, McNemar p=0.377 → n raised to 1500, p=0.017) because **FTS5 normalises `bm25()` by the row's TOTAL token count across all columns** — a per-column weight bounds what a path *match* scores but not what a path's mere *presence* costs. So the pre-specified fallback shipped: `document_files` as its own table, text ranking byte-identical to baseline **by construction**. Measured paired on the real corpus (n=1500): BM25 P@1 **0.395** vs vector **0.255**. **Grounding unchanged** — one shared `SearchHitResolver` re-resolves every hit against canonical and one `NodeState.searchable` allow-list is rendered into *both* the SQL filter and the re-check, so index and resolver cannot disagree; **trust gate untouched** (this only changes which real rows are eligible, never what may be said about them). **576 tests.** Two independent Opus reviews (whole-branch + a focused pass over Tasks 8–13, which had shipped with **no** per-task review) found 0 Critical and converged on a fix wave, all applied: an index path that ignored `PENSIEVE_DB` (so the project's own smoke-test recipe wiped the **live** index — now indexes follow the store they index), a matched-field snippet regression (a loose end matching only in its cited `quote` rendered with no highlight), the search index never refreshing on the watch path (⌘F silently missed work the app had just drained) now detached off the main actor, a latched `.building` flag, silent SQL-error swallows now logged, a tolerant typed `changedFiles` decode, diacritic-insensitive highlighting to match the tokenizer, MCP `limit` clamping + file-only queries + an explicit "already ranked, do not re-sort by `score`" contract, and **two vacuous tests de-vacuumed** (the injection-surface test asserted only `isEmpty`, so it passed with quoting removed entirely — mutation-verified both ways). **Still open — P3** (own spec, `backlog.md`): a paraphrase-only eval harness, **blocked on the user writing 30–50 paraphrase queries** as the gold set; both engines currently fail "find without remembering the words" (`vector` ≈0/8, `bm25` ≈2/8 on short queries), and the headline gold set uses full documents as queries, which flatters lexical matching. Spec/plan: `docs/superpowers/specs/2026-07-28-retrieval-eval-harness-design.md` + `docs/superpowers/plans/2026-08-03-retrieval-bm25-single-path.md`; measurements (probes committed, reproducible) in `docs/superpowers/measurements/2026-07-28-retrieval-recall/`. **Post-merge carry:** rebuild + reinstall to `/Applications` (the installed app is from **2026-07-19**, so the bundled `pensieve mcp` every Claude Code session calls does not yet have `search`'s new contract) + the human-verify list in `CONTINUE.md`. Also delete the orphaned `text-index.sqlite` in the support dir — the abandoned two-path branch's index, now dead.
- **Vector search removed — DONE** (2026-08-11): deleted the engine that lost on measurement. The embedder, its protocol seam, the `vec0` store, the incremental indexer, `SemanticQueries` (incl. `SemanticSearchScope` and the inert `floor`), the vendored `sqlite-vec` C target, the Settings toggle, the ⌘F "Related (experimental)" section, and the MCP `engine` field are all gone; **BM25 is the only retrieval path**. Three findings forced it: default-off **did not mean off** (a `true` persisted from July's default-on kept the vector engine live and measurably diluting MCP results — `search "focus filter spotlight"` returned the correct BM25 hit plus **three** vector rows, two of them unrelated projects at 0.886/0.878); a **disabled** feature still created its index and loaded a model asset on every launch (`b36f745`); and two engines was a standing tax on grounding — the retrieval branch had to extract a shared `SearchHitResolver` *because* the two had drifted. `EmbeddableItem`/`EmbeddableCorpus` **survive and moved** to `Search/` — that is the corpus BM25 reads, hygiene rules included. **Breaking:** MCP `search` items no longer carry `engine`, and `limit` now means `limit` (the `prefix(limit * 2)` over-allocation existed only to fit two engines). **P3 unaffected** — all four committed probes carry their own ~25-line embedder and import no PensieveKit vector type, so the eval owns its research dependency, which is the right home for it. Trust gate untouched (extraction never embedded). **576 → 538 tests** (38 deleted, BM25's own tests unchanged — verified across the whole branch). Spec/plan: `docs/superpowers/{specs,plans}/2026-08-11-remove-vector-search*`. **Post-merge carry:** rebuild + reinstall to `/Applications` so the bundled `pensieve mcp` stops advertising the old wire shape, and `defaults delete me.mazetti.pensieve app.semanticSearch` (now inert).
- **In-node find (⌘F) — DONE** (2026-08-11): the detail pane got its own find bar, and **the keybindings swapped** — **⌘F is now find-within-the-open-node; ⌥⌘F is search-everything** (the old global field, unchanged otherwise; `Go ▸ Search Everything`). Literal substring search (Safari semantics — no regex, no stemming) over everything the pane can render: node name, description, the narration, each open loose end's text, and **the transcript windows behind collapsed provenance rows**, highlighted in place with ⌘G/⇧⌘G walking matches in on-screen order. **Kit (tested, pure):** `FindMatcher` is now **the single definition of how Pensieve compares a query to captured text** (`[.caseInsensitive, .diacriticInsensitive]`, matching the FTS5 `remove_diacritics 2` tokenizer so typing `losung` finds `Lösung`) — `SnippetMaker` and the word-prefix options read it rather than declaring copies, closing the shipped two-paths-disagreed bug its own scar comment documented. `NodeFindDocument` fixes match order at construction to **on-screen** order, with a **pre-allocated slot per loose end** that the async sweep fills **in place** (appending would order ⌘G by file-completion order); a slot resolves to transcript units **XOR** the stored quote, never both (the quote is only rendered in the transcript-unavailable fallback and is by construction a substring of the cited message, so indexing both would double-count and point a match at a site that does not exist). The document honors **`showsLooseEnds`** — a childless focused strand shows its loose ends in the *middle* column and the detail renders no Loose Ends section, so indexing them would produce matches with no site to scroll to. `FindSession` tracks the current match by **identity (anchor + character offset), not ordinal**, so a background fill that inserts matches *ahead* of the user renumbers around them instead of silently moving them. `ProvenanceLoader` (`actor`) **parses each transcript once** and shares its parsed segments with the views — so document segment ordinals and rendered segment ordinals are identical *by construction* — invalidating on the file's **`(size, mtime)` fingerprint**, deliberately **not** `refreshToken`, which never bumps on the watch path (live sessions grow). `ProvenanceQueries` now has **one** two-part guard with **two** callers instead of two copies. **App (thin):** `NodeFindState` (`@Observable`, per-window — published via an explicit `FocusedValueKey` because this SDK's `@FocusedValue` has no `init(_ objectType:)` for a plain `@Observable` class, so Edit ▸ Find acts on the *focused* scene and each ⌘⌥N recall window owns its own find), `FindBar`, `FindCommands` (`CommandGroup(after: .textEditing)` — there is no `.find` placement), and `HighlightedText` as the **single** highlight renderer (`SnippetText` re-expressed over it). Find-driven expansion is per-window (`forcedExpansions`), never the app-wide `expandedLooseEndID`, and the scroll is **mount-driven** (`siteMounted`) rather than layout-timing guesswork. **Accepted trade-off:** *flatten-on-match* — a matched transcript segment renders as plain highlighted text with its raw Markdown syntax visible until the bar closes, because MarkdownUI 2.4.1 cannot style a substring inside a rendered block (`InlineNode`/`BlockNode`/`MarkdownContent.blocks` are all `internal`). **Measured reality that shaped the scope:** only **18 of 122** open loose ends on the `Pensieve` node still have a live transcript (85% resolve to the quote fallback), so the sweep buys less than the first draft assumed. **Trust gate untouched** — `TranscriptVocabulary.injectionMarkers` and `TranscriptParser.isInjectedOrCommand` are not read, written or referenced; `isUserPrompt` is reached only *transitively* through the existing guard. Read-only throughout. **582 tests** (+44). Spec/plan: `docs/superpowers/{specs,plans}/2026-08-11-in-node-find*`. **Human-verify carries** in `CONTINUE.md`. **Merged to `main` 2026-08-12** — see the merge note in the slice-A bullet below; the two branches restructured the same pane and one defect existed only in their combination.
- **"Where was I" — Claude Design review slice A — DONE** (merged to `main` 2026-08-12; **human-verified the same day**, outcome in `docs/superpowers/verify/2026-08-11-where-was-i-human-verify.md`): the reload-context pass over the surfaces you actually land on. **Detail pane:** an adaptive **state line** (`NodeMetaLine`) replaces the kind·state pair that said the same thing on every node; **cited loose ends now come BEFORE the recap**, and the recap is a **headerless closing paragraph** — a caps header announces a slot, and narration is allowed to return nil, so an empty slot would read as a failure where absence should read as nothing. **Middle column:** rows carry **recency + open count** instead of `kindLabel`, which had made the column read "Projekt / Projekt / Projekt". **Briefing:** weights **moved** work and collapses the **quiet** ones (five dormant projects used to outweigh the one that moved), and the hardcoded English fragments `"dormant \(days)d"` / `"\(n) since last visit"` — **absent from the String Catalog entirely** — are gone. **Kit (tested):** `NodeFacts`/`BriefingCard` carry `lastActivityAt`; a batched `NodeRowFacts` via two grouped aggregates (closing the long-standing "shared per-node latest-event + dormancy + open-count helper" carry). **The human-verify pass found one real defect, fixed:** hover-revealed thumbs were **unreachable** — `.onHover` sat on the row's outer `VStack`, but SwiftUI hit-tests only *rendered* content, so the `Spacer()` between text and thumbs was dead space: hovering the text revealed the thumbs, moving toward them ended the hover, and they vanished before they could be clicked (`.contentShape(Rectangle())`). It also caught **six String Catalog keys that never matched their literals**. **Four findings that belong elsewhere are filed in `backlog.md`** (narration can still emit a facts-dump; `Du` vs `user` label one speaker twice; `Pensieve öffnen` truncates in German; Full Keyboard Access tab order is erratic). **589 tests** after the in-node-find merge. Spec/plan: `docs/superpowers/{specs,plans}/2026-08-11-where-was-i*`. **Merge note (in-node find × slice A):** slice A moved the recap below the loose ends, but `NodeFindDocument.make` still emitted the narration slot *before* them — ⌘G walks document order and the type's contract is "order matches on-screen order", so the slot moved and `documentOrderFollowsOnScreenOrder` pins the new order. **Neither branch could have caught this alone.** The merge also pushed `AppModel.swift` to 404 lines (`swiftlint --strict`, which CI runs, caps at 400), so `commitNewNode`/`updateNode` moved into `AppModel+Organizing.swift` beside the rest of the organizing writes. **`xcodegen generate` is required after this merge** — the project is generated and gitignored, and five app files are new; without it the build fails with `cannot find 'FindCommands' in scope`.
- **Loose ends can end — `open` / `done` / `dropped` — DONE** (merged to `main` `a1c649a`, 2026-08-13): the backlog's "single highest-value idea", and a data-model change rather than chrome. A loose end was open forever, so **"Als Nächstes 155" and "Ruhend 112" never shrank and neither number meant anything**; a project with no open work and no recent activity read as *neglected* when it was *finished*. **Kit (tested):** `LooseEndStatus` (`open`/`done`/`dropped`) — a real `RawRepresentable` enum over the `status` column that has shipped since Phase 1B and that **no production code had ever written** (all 968 rows held `open` when the migration was written); on-disk strings unchanged, so the type change needs **no migration** and **v12 adds only a nullable `resolvedAt`**, which the Completed feed orders by (the source event's date cannot answer "what did I finish lately"). **`LooseEnd.isOpen` is deliberately not edited** beyond retyping its literal — `status.eq(.open)` already excludes both new cases, which is what carries resolution into the detail view, the menu-bar count, App-Intents facts, What's-Next ranking, the search corpus and Spotlight **without editing any of them** (`openSQLPredicate` byte-identical; `looseEndOpenPredicatesAgree` still passing). One write verb `resolve` (stamps `resolvedAt` on close, clears it on reopen, and takes an explicit `restoringResolvedAt` **for undo only**, so an undone flip restores the *pair* instead of re-stamping week-old work to the top of Completed) + `resolveAllOpen` (per-node bulk, scoped by **`isOpen`** so it closes exactly what the dialog counted, returning the ids it changed so one undo reopens exactly that set). Three feeds (`openAcrossNodes` — **suggested-salient first, then oldest**, because measurement showed oldest-first is "grind through three repos"; `closedAcrossNodes`; `closed(nodeID:)`) sharing one extracted `attachEvents`, plus a count-only `openCountAcrossNodes` for the sidebar badge. **`isActionable` is two-part on measured grounds** — `openLooseEnds > 0 || closedLooseEnds == 0` — because **123 of 162 active nodes are git-only** and can never produce a loose end, so the naive `openLooseEnds > 0` would have removed 130 nodes on day one; applied at the **three** surfaces that answer "what should I pick up next" (`SmartLists.whatsNext`, MCP `rankedContext`, `pensieve next`) and **never inside `ranked`**, which also feeds Dormant/Recently Active. **Search:** index **schema v4** carries each document's own `item_status`, and one `LooseEndStatus.searchable(includeClosed:)` allow-list is rendered into **both** the FTS5 SQL filter **and** the canonical re-check in `SearchHitResolver` (whose hardcoded `isOpen` was the one place the two could disagree); closed ends joined the corpus (👎 still excluded — that asymmetry is deliberate: 👎 asserts the text was never a loose end, a closed end *was* real work); `updateStatus` writes the UNINDEXED column in place. **App:** resolve verbs on a row via one shared `LooseEndStatusMenu` (context menu + swipe) and `LooseEndResolveCommands` (⌘⏎ / ⌥⌘⏎ / ⇧⌘⏎ over the focused feed row, published as a **`.focusedSceneValue`**), each with undo; **Loose Ends** + **Completed** sidebar buckets (`SidebarSelection` cases, *not* `SmartListKind` — that enum's `itemsKeyPath` returns nodes; `DeepLink` untouched) where ↑↓ walks the queue and the detail pane follows **without moving the sidebar selection**; a collapsed **Done · N** record rendered **last** in the detail pane (load-bearing: in-node ⌘F indexes per-loose-end slots in on-screen order, so closed rows inside the Loose Ends section would recreate the slice-A × in-node-find defect); ⌥⌘F's second scope widened to **Include Archived & Closed** (one control, two orthogonal kernel dimensions) with closed rows badged. **MCP** `search` reaches closed ends and reports `closed`. **Two independent Opus reviews caught two defects that broke the feature in use** — (1) resolving updated **no list until ⌘R**, because the feeds key their reload on `refreshToken` which `refresh()` deliberately never bumps (fixed with a dedicated `looseEndRevision`; `DetailView` now separates its three entry kinds so a resolve reuses cached prose instead of spending an LLM call), and (2) bulk close **closed more than its dialog counted** (`status`-only vs `isOpen`, stranding 👎 ends on no surface at all) — plus **a false premise the spec, the plan and three comments all shared:** a stale index does **not** shrink the result page (`SearchQueries` over-fetches ×8 and grows `k`, backfilling around the row the resolver drops); the real hole is a stale `done` making live work **unfindable** in SQL, where the resolver never sees a candidate. **Two vacuous tests were found by running the mutation, not by reasoning** — the plan's index-freshness test *and* its first replacement both passed with `updateStatus` deleted; the two that shipped are mutation-verified. Follow-up in its own commit (`ecd96ff`): **`SalienceSuggester` also dropped its `status == open` filter**, closing spec D9's other door — Review Suggestions only shows ends that already carry a `labelSuggestion`, so a closed end the suggester never proposed for could never be audited, and 846 of 968 items are unlabeled. **Trust gate untouched. 659 tests.** Spec/plan: `docs/superpowers/{specs,plans}/2026-08-13-loose-end-resolution*`. **Human-verify carries in `CONTINUE.md`** (the burn-down loop, ⌘Z after a mis-key, a project actually finishing, German in situ).
- **Talk to the system, stage 1 (three-pane slice 5) — DONE** (merged to `main`, 2026-08-13, through `8d2ba0b`): describe a strand in a sentence, get a node — the app's first write path where the **user**, not the model, authors identity, alongside `Ingester.nameStrand`'s existing model-authored one. **Kit (tested):** `TextQuality.sanitizeLabel` moved out of `Ingester` so it and the new `NodeLabeler` share one gate, plus a new deterministic `TextQuality.shorten` — a word-boundary label built from the user's own words, never a fabrication. `NodeLabeler.label(for:provider:)` routes by detected language (`NLLanguageRecognizer`): **English goes to the on-device model; every other language takes the deterministic arm** — measured, not assumed. The model *translates* non-English input rather than labeling it (a German delayed-train complaint came back "Train Advertisement Claim"; an explicit "do not translate" instruction made it worse); evidence in `docs/superpowers/measurements/2026-08-13-slice5-label-quality/`. `NodeLabeler` returns nil **only** on empty input, so the modal always has a name and Save is never stuck disabled. `NodeFields.description` is now **compulsory** (no default — a default would silently blank an existing description on every `update`), and `NodeCommands.update` writes it: the app could not set a description before this. **App (thin):** the New/Edit modal gained a Description field and a Suggest-name button (`AppModel.suggestName` reuses the retained `descriptionProvider`); a new node now lands **under the current selection** (both quick-add paths previously passed `nil`, landing every node at top level), falling back to top level for an archived selection. **Deliberate deviation from the spec, already decided:** the spec called for two fields (a prompt and a description); they are the same text, so it is one Description field plus a Suggest button, which also keeps Save's `.defaultAction` uncontested (no `onSubmit` in a sheet). **No `EvalTask` registered**, deliberately — same shape as the pre-existing, also-unregistered `Ingester.nameStrand`; logged in `backlog.md` ▸ "Naming has no eval coverage" covering both call sites, not just this one. **Deferred by measurement, not by argument:** model-assisted *parenting* — the original design had BM25 shortlist candidate parents from the typed sentence, but `FTSQueryBuilder` AND-joins every term, so a realistic quick-add sentence retrieves **zero** rows; the alternative that does work (OR-aggregating across all three hit kinds) is recorded for a follow-up gated on its own committed measurement (`backlog.md` ▸ "Deferred, with triggers"). **681 tests** (+9: 2 moved from `Ingester`, +3 `shorten`, +5 `NodeLabeler`, +1 `NodeCommands`); lint clean (0 violations). Spec/plan: `docs/superpowers/{specs,plans}/2026-08-13-talk-to-system-slice5*`. **Verification note:** the app-side runtime behaviour here was verified by code inspection and the compiler, **not** by the project's standard smoke-launch — a review during this run reproduced that the recipe creates no store file and exercises no app code at all (`AppModel.start()` runs from `.task` on a rendered view body; a backgrounded direct-exec of the inner Mach-O never renders one), a pre-existing gap in the documented recipe that predates and is not specific to this slice. Human-verify carries are in the plan's own ledger.
- **Translation settings — pack management, measured coverage, and an explicit backfill — DONE** (branch `worktree-translation-settings`, 2026-08-14, through `1ac2cab`; **not yet merged to `main`**): the control surface the on-device translation slice never built, and the reason turning German on looked like a broken feature. Measured on the live store on 2026-08-14: **2 stored translations, both narration, against 1,294 distinct translatable texts** — and `nodeName`/`nodeDescription`, two of the three fields `EmbeddableCorpus.gather` looks up, had **no production writer at all** (only the eval probe ever wrote them), so a user who switched to German saw English node names forever with nothing saying why. **Kit (tested):** `TranslatableUnit`/`TranslatableCorpus.gather` — the `(field, sourceText)` set the search corpus will look up, serving as **both** the coverage denominator and the backfill's work list so the number shown and the work done cannot disagree; eligibility is **extracted, not restated**, into `EmbeddableCorpus.corpusNodes`/`corpusLooseEnds` (active + archived nodes; `noise`-labelled ends excluded) because this project's recurring defect is two paths that were supposed to agree and drifted (`SearchHitResolver` had to be extracted for exactly this, and the loose-end-resolution branch found the index filter and the canonical re-check disagreeing about `isOpen`), pinned by a **two-direction property test against `gather` itself**, each stated with the mutation it must fail under. `TranslationCoverage.measure` counts by **distinct text, not by row** — `TranslationStore` is keyed `(field, source_hash, language)`, and the eval run measured 278 node names collapsing to 272 distinct strings, so a row denominator would sit permanently short of its total and read as failure; `off` yields zeros **without opening the store**. `TranslationBackfill.run` is explicit, **serial, idempotent, resumable by construction, cancellable, and never retries a nil** — four of those five are scars from the eval run that did this work once (`measurements/2026-08-12-translation-ranking/`): the generator lacked check-then-skip, died at 522/870, and had to be given it before it could be re-run; each `put` commits its own row, so there is no checkpoint to corrupt. `TranslationTarget.supported = ["de"]` is **gone** — its stated cost ("each language doubles a slice of the search index, which is a measured cost") was **refuted by its own pre-registered ranking gate**, which shipped with English P@1 statistically indistinguishable from baseline (**McNemar p = 1.000**), and only one target is live at a time, so the doubling is bounded at exactly the case that gate cleared; `resolved()` now validates **shape** (ISO 639 subtag) rather than membership, and `displayName` uses `forIdentifier:` **not** `forLanguageCode:`, which renders `zh`/`zh-HK`/`zh-TW` as three identical `中文`. **App (thin, new `TranslationSettingsSection.swift`):** a picker over the **29 offered targets measured on this machine** (38 supported, of which 9 English variants report `.unsupported` for en→en and drop out **by status alone** — no hand-maintained exclusion list), each row its own endonym, persisted as `minimalIdentifier` (`maximalIdentifier` would store `zh-Hans-CN` for a user who chose `zh`; a bare language code would collapse `zh-HK` onto `zh` — the wrong script); an adaptive pack-status line; a **Download** button that mounts the `.translationTask` only while a download is running, so the OS download request stops being a permanent resident of the view hierarchy that re-fired on every Settings open; a link out to `com.apple.Localization-Settings.extension`, the honest ceiling of "manage" since pack deletion is OS-only; and the coverage row with one `Translate remaining`/`Stop` button, disabled when no pack is installed. **Two review-found defects, both the interesting part:** (1) the download could mount for a language the user never pressed Download for — `isDownloading` was a `Bool` reset only **inside the async body of a `.task(id:)`**, and a `.task(id:)` body never runs synchronously with the state change that alters its id, so the render after a mid-download picker switch still saw `true`; fixed by making the state `downloadingLanguage: String?` and gating on `downloadingLanguage == translationTarget`, which makes the guard **synchronous with the picker write**, with the completion re-checking too because `try? await session.prepareTranslation()` swallows cancellation; (2) coverage carried **no record of which language it was measured for**, so a click inside the re-measure window handed language A's `missing` list to a backfill resolving language B — fixed by storing `(language:coverage:)` and requiring the match in both the view and `startTranslationBackfill`. Note the shape of (2): the plan said it copied `AppModel+Search.runSearch` but omitted **the very part of `runSearch` that exists for this**, its monotonic `searchToken` — the same stale-async-write class already fixed twice here (slice 3a's narration render window, `DetailView`'s `loadedNodeID == node.id`). **The spec required a cancel the plan omitted** ("changing the target language cancels a run in flight"), wired as `.onChange(of: translationTarget)` and deliberately **not** `.task(id:)`, whose body also runs on first appear and would kill a run every time Settings merely reopened — while "closing Settings does not kill a run" is a requirement of the same task; an Opus review upheld it with evidence: 22 `.onChange` sites in this app all rely on the `initial: false` default, and `GeneralSettingsTab.swift:26` would register/unregister the SMAppService login item on every Settings open if it fired initially. The run is a **stored `Task.detached` cancelled directly** — a detached task does not inherit cancellation, so cancelling a wrapping parent would leave it going while the UI said it had stopped — and `translationBackfillProgress != nil ⟺ translationBackfillTask != nil` at every main-actor quiescent point (both set in one synchronous run, cleared in one synchronous pair with no `await` between), which makes "Stop then immediately restart" **unreachable rather than merely guarded**: while a task exists the progress branch renders and there is no button to press. **Two literals differ from the plan's key table:** the running row is `Translating %lld of %lld…` on **its own key**, because Kit's counter advances on every unit **attempted**, not every one written (a pack deleted mid-run would otherwise count to "1294 of 1294 translated" having written nothing), and `Everything is translated.` was deleted as redundant with the count beside it. Nine German keys hand-authored, `Prepare translation` deleted (the catalog is IDE-populated only, so this is hand work, verified through `plutil -p` on the built bundle). **Correction to the plan's Global Constraints:** the app deployment target is **26.0** (`project.yml:9`), not 15.0 — so the `#available(macOS 26, *)` guards and the "requires macOS 26" fallback text are **unreachable in this build**, kept deliberately because the spec's degradation table calls for them and they mirror surrounding code. **Trust gate untouched, structurally:** no case added to `TranslationField`, nothing reads a `quote` or a transcript message, no `LLMProvider` involved. **No `EvalTask`** — nothing here is LLM-backed; translation is the on-device `Translation` framework, which exposes no model choice, so the `registry ↔ config` test is unaffected. **703 tests in 12 suites** (+18 over a 685 baseline: 5 + 5 + 5 in Tasks 1–3, +3 in Task 4); lint 0 violations; app builds; embedded-CLI smoke passes. **Verification limit, stated plainly:** the app target has no unit tests and this project's documented smoke-launch renders no view body, so **not one line of `TranslationSettingsSection` or the three new `AppModel` methods has executed** — everything about the app half is static analysis plus the compiler. `make run` was deliberately **not** run: it replaces the live `/Applications/Pensieve.app` and re-mints the bundled sync helper's cdhash, so the install and the interactive checklist are the human's (checklist in the plan). Spec/plan: `docs/superpowers/{specs,plans}/2026-08-14-translation-settings-coverage*`; deferred follow-ups and this run's minors in `docs/superpowers/backlog.md`.
- **Next: two open tracks (each its own spec).** **(A) three-pane app** *(recommended)*: slice 5 (talk-to-system stage 1) is **DONE** — next is slice 6 (forks, gated on the unbuilt fork-capture backend — brainstorm that backend first). **(C) findability / OS-integration:** in-app find, menu bar, `pensieve://`, App Intents + Spotlight, Focus filters, Spotlight loose-end indexing, **retrieval P1+P2′ (BM25)** and **in-node find (⌘F)** shipped; **semantic/vector recall was built, measured worse, and removed (2026-08-11)**. Remaining — **P3 the paraphrase eval harness** (blocked on the user's gold set) and **transcript-passage chunking** (own spec). **Track B is DONE** (cloud provider + organizing-writes error surfacing). **Widgets + CloudKit stay BLOCKED** on a paid Apple Developer team (App Groups / Team-ID entitlement — one gate unblocks the whole extension family). See `docs/superpowers/backlog.md` **Roadmap**.
- **Long-term (intended, not scheduled)** — the full vision, none foreclosed: CloudKit sync + iOS companion; more OS-integration surfaces (widgets, deeper Spotlight/semantic search, Siri-Shortcuts depth); FSEvents real-time monitoring; additional non-code source types (Notion, Entra, browser); cross-project dependency graphs / analytics. Forward ideas: **forks as first-class**, **talk to the system stage 2** (a conversational agent; stage 1 shipped), statistical theme discovery, proactive project suggestion. (The `LSUIElement` hide-dock toggle and the resident background `SMAppService` agent both shipped.) Full detail: `docs/superpowers/backlog.md` (Roadmap + deferred ledger) and `docs/superpowers/specs/2026-07-03-pensieve-mvp-design.md`.

## Build & test

- **Use the `make` targets — they are the canonical way to run every task here.** Reach for the raw `swift`/`xcodebuild`/`xcodegen` command only when debugging the recipe itself, or when you need a flag no target exposes (`swift test -c release`, two `--filter`s). `make` lists them: `make all` = lint + test + build + embedded-CLI smoke, i.e. everything CI checks, in CI's order; `make test [FILTER=<name>]`, `make lint`, `make generate`, `make build` (runs `generate` first), `make cli`, `make smoke`, `make install` (replaces `/Applications/Pensieve.app`, refusing while the app is running), `make run` (`all` + `install` + launch the installed bundle), `make clean` (build products only — never the installed bundle). The bullets below stay the source of truth for *why* each flag is there — they explain the recipes, they are not a second way to invoke them. `make build`/`make cli` pass `-skipMacroValidation -skipPackagePluginValidation`, so they don't need the machine-wide macro-trust `defaults write`.
- **Launch the app with `make run`, not Spotlight.** Every worktree `xcodebuild` registers another bundle under `me.mazetti.pensieve`, and LaunchServices picks between them by rules of its own — a stale copy on screen reads as a regression (on 2026-08-13 a month-old DerivedData build looked like Settings had "lost its tabs"). `make run` names the bundle. If Spotlight ever surfaces the wrong one again, `lsregister -dump | grep Pensieve.app` lists the registrations and `lsregister -u <path>` drops a stale one (`-kill` was removed in macOS 26).
- **The steps are cached against their inputs** — a no-change `make all` is ~0.1s vs ~30s cold. Records live in the gitignored `.make/` (lint/test/smoke); `build`/`cli` use their real products as the record, so deleting build products by hand forces a rebuild instead of a stale pass. Source *directories* are prerequisites alongside files, which is how an added/removed file invalidates the generated `.xcodeproj` (mtime comparison alone is blind to a deletion). `Package.resolved` is deliberately **not** an input — xcodebuild rewrites it on every app build, which would invalidate the test record every time. `make test FILTER=<name>` records nothing (a filtered run proves one test, not the suite). Force with `make -B <target>`; discard everything with `make clean`.
- **Run the test suite with `make test`** (optionally `FILTER=<name>`), which dispatches to `swift test` — Xcode 26.6 is the active toolchain, so Swift Testing loads natively and needs no wrapper (`scripts/test.sh` was exactly that wrapper and was deleted 2026-08-13; the many references to it in `docs/superpowers/plans/` are historical record, not live recipes). Running `swift test` by hand works but writes no `.make/test` record, so the next `make all` re-runs the suite. `swift build`/`swift run` work normally.
- **Building the app with `xcodebuild` needs the SPM macro plugins trusted** (SQLiteData → swift-structured-queries / swift-perception macros). A fresh machine's first `xcodebuild` fails until you trust them — either click **Trust & Enable** when Xcode prompts, or run once (per-machine, not a repo change): `defaults write com.apple.dt.Xcode IDESkipPackagePluginFingerprintValidation -bool YES` and `defaults write com.apple.dt.Xcode IDESkipMacroFingerprintValidation -bool YES`.
- The CLI is now an Xcode `tool` target (**target `PensieveCLI`, `PRODUCT_NAME: pensieve`** — the target is renamed to avoid a case-insensitive-filesystem collision with the app target `Pensieve`; the shipped binary is still `pensieve`), embedded + **ad-hoc-signed** at `Pensieve.app/Contents/Helpers/pensieve`. Build it with `make cli` (dispatching to `xcodebuild -scheme PensieveCLI build`; `make build` uses the app scheme `Pensieve`, which also builds + embeds it) — **`swift run pensieve` no longer exists**. PensieveKit + tests stay SwiftPM (`make test`); tests honor `PENSIEVE_DB` and `PENSIEVE_CAPTURE_DB` env overrides to point at temp SQLite files.
- If a build dies with a SwiftSyntax/macro linker error, `rm -rf .build` and retry (still recurs intermittently; disk currently has headroom — ~44 GB free).
- **Gotcha:** external callers (git hooks, `~/.claude/settings.json`, `claude mcp add`) resolve the CLI via the `~/.local/bin/pensieve` symlink, not a `PATH`-found binary — keep `~/.local/bin` on `PATH` for the bare-token `claude mcp add pensieve` case. The launch-time symlink auto-create runs only from the installed `/Applications` app (guarded off `.build` paths).

## Observability

Structured logging is via `os.Logger` (subsystem `me.mazetti.pensieve`). Useful commands:

- **NOTE: `log` is shadowed by a shell function here — prefix these with `/usr/bin/`** or they print nothing and look like "no events".
- **Stream live (all categories):** `log stream --predicate 'subsystem == "me.mazetti.pensieve"' --level debug`
- **Recent logs (last 2h):** `log show --predicate 'subsystem == "me.mazetti.pensieve"' --last 2h --style compact`
- **Single category:** `log show --predicate 'subsystem == "me.mazetti.pensieve" AND category == "extraction"' --last 1h`
- **JSON export (for parsing):** `log show --predicate 'subsystem == "me.mazetti.pensieve"' --last 2h --style ndjson`
- **MetricKit diagnostics (crash/hang payloads):** `ls ~/Library/Logs/Pensieve/diagnostics/`
- **macOS crash reports:** `ls ~/Library/Logs/DiagnosticReports/Pensieve-*.ips`

Categories: `sync`, `extraction`, `llm`, `ingest`, `discovery`, `app`.

Full guide with debugging recipes: `docs/observability.md`.

## Architecture (as built)

Two SQLite databases, deliberately separate:
- **Capture spool** (`capture.sqlite`) — dumb, append-only, WAL, never synced. Git hooks / `pensieve capture-*` append one raw row and exit. **The capture path is sacred: fast, fire-and-forget, and must never block or break a git commit.**
- **Canonical store** (`pensieve.sqlite`) — rich model via **SQLiteData (GRDB-backed)**, UUID PKs, STRICT tables. The only store that will ever sync (CloudKit, later).

Flow: `git hook / CLI → spool → Ingester.drain() → canonical events`. The **ingester** is the only writer of the canonical store; it enriches git commits (via `git show`) and Claude Code sessions (by reading the on-disk `.jsonl` transcripts) into project-attributed `Event`s.

**Core principle — a Project is an area of work, NOT a directory.** Git repos are one *source type* (`SourceKind.gitRepo`); Claude Code sessions another (`SourceKind.claudeCode`). Sources bind many-to-one to a project; `pensieve group` merges. Attribution is by *canonicalized filesystem path → source → project*.

Layout: `Sources/PensieveKit/{Model,Store,Capture,Transcript,Ingest,Intelligence,LLM,Discovery,Daemon,Sync,Query,Support}` (the framework — all real logic), `Sources/pensieve/` (thin CLI over PensieveKit), `Sources/PensieveApp/` (thin SwiftUI three-pane app over PensieveKit), `Tests/PensieveKitTests/`.

## Conventions & gotchas

- **Name things explicitly — no abbreviations, no single letters.** Write `database`/`node`/`looseEnd`/`event`/`payload`, not `db`/`n`/`le`/`ev`/`p`. The terse names presently scattered through the codebase are **historical Claude output, not the project's style**, and are being renamed; do not treat them as a pattern to match, and do not relax SwiftLint's `identifier_name` rule to accommodate them. Genuine wire-format keys (JSON/MCP `snake_case` such as `node_id`, `max_tokens`) are the one exception and belong behind explicit `CodingKeys` so the Swift property stays camelCase.
- **SwiftLint is enforced in CI (`swiftlint lint --strict`)** with `.swiftlint.yml` at the repo root. Config encodes real conventions; it is not a place to silence findings that should be fixed in code.
- **SQLiteData 1.6.6 predicates use `.eq(x)`, NOT `== x`** (e.g. `.where { $0.name.eq(name) }`). `==` is `unavailable` and won't compile.
- Table/column names must match `@Table` property names exactly; tables are `STRICT`; PKs are `UUID` (keeps CloudKit reachable).
- Kind strings live in `CaptureKind` / `SourceKind` (`CapturePayloads.swift`) — reuse the constants, don't hardcode.
- Don't use a shared mutable `static` `ISO8601DateFormatter` (Swift 6 concurrency); use a local instance or `Date.ISO8601FormatStyle`.
- **No Python, ever.** Swift only.
- LLM work (Phase 1B): I have a Claude *subscription*, **no API key** — go through an `LLMProvider` protocol whose default shells out to `claude -p`. Stay provider-agnostic.
- **New LLM-backed tasks must register an `EvalTask`** and get their default model from `pensieve eval` (on-device-first), not a hand-picked constant. See `Sources/PensieveKit/Eval/README.md`. A `registry ↔ config` test fails the suite otherwise.
- **The app (`Sources/PensieveApp/`) has no unit tests** — it's an Xcode app target, not covered by `PensieveKitTests`. Verify with an `xcodebuild` build + a **non-blocking** smoke-launch of the **inner binary** (`./.build-xcode/Build/Products/Debug/Pensieve.app/Contents/MacOS/Pensieve`, background + `kill`; forward throwaway `PENSIEVE_DB`/`PENSIEVE_CAPTURE_DB`). Keep derivation logic (bucketing, tree/card building, ranking) in **tested PensieveKit**; keep views thin. App reads are read-only; the only canonical writer is still `Ingester.drain()`.
- **The app is a real `.app` bundle built by XcodeGen + Xcode** — `project.yml` (source of truth) defines a macOS app target linking PensieveKit as a local SwiftPM package. Build: `make build`, which dispatches to `xcodegen generate` then `xcodebuild -project Pensieve.xcodeproj -scheme Pensieve -configuration Debug -derivedDataPath ./.build-xcode build`; the app lands at `./.build-xcode/Build/Products/Debug/Pensieve.app`. Smoke-launch the **inner binary** (`…/Contents/MacOS/Pensieve`) so `PENSIEVE_DB`/`PENSIEVE_CAPTURE_DB` env is forwarded. The bundle is `.regular` by default (no activation-policy workaround) and its icon is the bundled `icons/Pensieve.icon` compiled by `actool` (no runtime `applicationIconImage`). `Pensieve.xcodeproj/` + `.build-xcode/` are gitignored; regenerate with `xcodegen generate`. `swift run PensieveApp` no longer exists.

## How we work here

Design-first. For any feature: brainstorm → write a spec/plan under `docs/superpowers/` → execute with review checkpoints (subagent-driven). Surface tradeoffs; keep changes surgical and minimal (YAGNI). Grounded-with-provenance is the north star for the intelligence layer — every AI-surfaced "loose end" must cite real captured text or it doesn't appear.

**Platform primitives first.** Always prefer the first-party / OS-native mechanism over a custom implementation when one exists — SwiftUI `App`/`Scene` lifecycle and `.commands` over a hand-built `NSMenu`, `WindowGroup` window restoration over manual frame autosave, Foundation/AppKit facilities over reinvented equivalents. Reach for a custom path only when the native one is proven insufficient, and say why. This keeps Pensieve idiomatic, small, and durable across OS versions.

## Where to read next

- `docs/superpowers/specs/2026-07-03-pensieve-mvp-design.md` — the approved design (full intent, all phases).
- `docs/superpowers/plans/2026-07-03-pensieve-phase1a-capture-ingest.md` — the executed 1A plan.
- `docs/superpowers/phase-1a-outcome.md` — **what 1A actually shipped + concrete design inputs for Phase 1B** (read this before planning 1B).
